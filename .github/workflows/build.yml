name: Build Bytecode

on:
  push:
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses : .github/workflows/setup.yml

      - name: Build
        run: ./gradlew clean build --debug